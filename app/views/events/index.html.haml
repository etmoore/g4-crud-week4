%button
  = link_to "New Event", new_events_path
%table.table
  %thead
    %tr
      %th Date
      %th Location
      %th Description
      %th Capacity
      %th Requires id?
  %tbody
    - @events.each do |event|
      %tr
        %td=event.date
        %td=event.location
        %td=event.description
        %td=event.capacity
        %td=event.requires_id
